# Root env for Docker Compose build
# Copy to .env and set values before running `docker compose up -d --build`

VITE_GOOGLE_MAPS_API_KEY=
VITE_GOOGLE_MAPS_ID=
VITE_MAPS_LANGUAGE=en
VITE_MAPS_REGION=CA
